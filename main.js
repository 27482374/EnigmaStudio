const reduce=matchMedia("(prefers-reduced-motion: reduce)").matches;
if(!reduce){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("show");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".heading,.project,.next,.about-grid,.principles div,.person,.join,.contact-content").forEach((e,i)=>{e.classList.add("reveal");e.style.transitionDelay=Math.min(i*.05,.3)+"s";io.observe(e)});const shape=document.querySelector(".shape");window.addEventListener("pointermove",e=>{if(innerWidth<900)return;const x=(e.clientX/innerWidth-.5)*8,y=(e.clientY/innerHeight-.5)*8;shape.style.marginLeft=x+"px";shape.style.marginTop=y+"px"},{passive:true})}