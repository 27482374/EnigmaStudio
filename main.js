const reduced=window.matchMedia("(prefers-reduced-motion:reduce)").matches;if(!reduced){const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");observer.unobserve(e.target)}})},{threshold:.12});document.querySelectorAll("section,.project-card,.values article,.person,.join-box").forEach(e=>{e.classList.add("reveal");observer.observe(e)});const orb=document.querySelector(".orb");window.addEventListener("pointermove",e=>{if(innerWidth<850)return;const x=(e.clientX/innerWidth-.5)*12,y=(e.clientY/innerHeight-.5)*12;orb.style.marginLeft=x+"px";orb.style.marginTop=y+"px"},{passive:true})}